{
  "name": "base64url-ts2",
  "version": "2.0.1",
  "description": "For encoding to/from base64urls. Updated to work properly with TypeScript projects.",
  "main": "index.js",
  "typings": "dist/base64url.d.ts",
  "files": [
    "dist/",
    "index.js"
  ],
  "scripts": {
    "build": "tsc --project .",
    "clean": "rm -f dist/*",
    "test": "npm run clean && npm run build && tap test/*.test.js",
    "prepublish": "npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/gradecam/base64url.git"
  },
  "keywords": [
    "base64",
    "base64url"
  ],
  "author": "Brian J Brennan",
  "license": "MIT",
  "readmeFilename": "README.md",
  "devDependencies": {
    "@types/node": "^7.0.12",
    "tap": "6.1.1"
  }
}
